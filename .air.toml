root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/main ."
  entrypoint = "./tmp/main"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", ".git"]
  delay = 500

[log]
  time = true

